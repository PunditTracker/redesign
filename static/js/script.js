$(function() {
    // $(window).scroll(function() {
    //     var $hero = $('.hero'),
    //         $bg = $('.hero > .background'),
    //         difference = $(window).scrollTop() - $hero.offset().top,
    //         percentComplete = Math.min(Math.max(0, difference)/$hero.height(), 1);
    //     $bg.css({"margin-top": 200*percentComplete});
    // });
});

